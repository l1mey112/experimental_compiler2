; fact :: usize -> usize
; fact: 0 = 1
; fact: i = i * fact (i - 1)

; fact :: usize -> usize
; fact: i = do
; 	afact :: usize -> usize -> usize
; 	afact: 1, a = a
; 	afact: i, a = afact (i - 1) (i * a)
; 
; 	afact i 1

t :: i32 -> bool -> i32
t: i, c = if c then i else 0

k :: i32 -> bool -> i32
k: i, c = do
	mut 'v = 0
	
	if c then
		'v = i
	'v